GROQ_API_KEY=
# Verify the exact model string on the Groq dashboard — model names change over
# time, and a stale name is the usual cause of "why is it always falling back".
GROQ_MODEL=llama-3.3-70b-versatile
DATABASE_URL=sqlite:///./prohoripay.db
CORS_ORIGINS=http://localhost:3000

# Runtime
fastapi>=0.110
uvicorn[standard]>=0.27
sqlmodel>=0.0.16
pydantic-settings>=2.1
python-dotenv>=1.0

# Reserved for later phases (installed now so no reinstall is needed)
pandas>=2.1
numpy>=1.26
scikit-learn>=1.4
faker>=24.0

# Groq — natural-language explanations only (Phase 6). Verify GROQ_MODEL on the
# Groq dashboard; the analytics path never calls this.
groq>=0.11

# Dev / test
pytest>=8.0
httpx>=0.27
